# Core runtime — this file is what Vercel installs, so it is kept lean on
# purpose. The model + web app + .txt/.md/.docx/native-PDF uploads run on
# these alone.
numpy>=1.23
Flask>=3.0
pdfplumber>=0.10           # native-text PDFs
python-docx>=0.8.11        # .docx reports

# Optional OCR support for scanned PDFs / images (LOCAL use only — OCR is
# disabled on the Vercel deployment). Install with:
#   pip install -r requirements-ocr.txt
